import React from "react";
import FilePicker from "../FilePicker";

import { ModalBody } from "@chakra-ui/react";

const Body = () => {
  return (
    <ModalBody>
      <FilePicker />
    </ModalBody>
  );
};

export default Body;
